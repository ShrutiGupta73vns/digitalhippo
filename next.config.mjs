/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
			'localhost',
            'digitalhippo-production-54a3.up.railway.app',
		],
    }
};

export default nextConfig;
